<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChangeLog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script>
        function filterChangelog() {
            let input = document.getElementById("changelogSearch").value.toLowerCase();
            document.querySelectorAll(".changelog-entry").forEach(entry => {
                entry.style.display = entry.innerText.toLowerCase().includes(input) ? "" : "none";
            });
        }
    </script>
</head>
<body>
    <div class="container mt-4">
        <div class="mb-3">
            <input type="text" id="changelogSearch" class="form-control" placeholder="🔍 Änderungen suchen..." onkeyup="filterChangelog()">
        </div>

        <div class="card shadow-lg p-4">
            <h2 class="text-center mb-4">ChangeLog</h2>
            <div class="table-responsive">
                <table class="table table-hover text-center align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th>Version</th>
                            <th>Datum</th>
                            <th>Beschreibung</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="changelog-entry table-success">
                            <td>1.0.1</td>
                            <td>26.01.2025</td>
                            <td>Kleine Änderungen an der Datenbank</td>
                            <td><span class="badge bg-success">Veröffentlicht</span></td>
                        </tr>
                        <tr class="changelog-entry table-success">
                            <td>1.0.0</td>
                            <td>25.02.2025</td>
                            <td>Erste stabile Version veröffentlicht</td>
                            <td><span class="badge bg-success">Veröffentlicht</span></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
